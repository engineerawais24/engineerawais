# Claude Code personal/project tooling settings (local-only, not for the repo)
.claude/

# Python (backend dev environment)
.venv/
.pytest_cache/
__pycache__/

# Local SQLite databases — user data, never committed
*.db

# Environment / secrets — never committed (only *.example placeholders belong in git)
.env
**/.env
.env.*
!.env.example
!**/.env.example
